# 承接自舊 repo 的背景報告

這三份是 CHBR-D-26-01681 revision 期間（2026-09-13 ～ 09-14）在舊 repo
`C:\Users\wuc120\Code` 產出的工作紀錄，**原件仍留在舊 repo，此處為副本**。

它們不屬於本專案的執行過程，但記錄了「為什麼需要從零重做」的完整證據，
論文的方法論修訂章節會需要引用：

| 檔案 | 內容 |
|---|---|
| `CHBR-R1_階段報告_20260913.md` | 首次系統性盤點原始實驗的缺陷：資料洩漏、標籤解析順序偏誤、`num_ctx` 未設定、勝出 prompt 未持久化等 |
| `CHBR-R1_階段報告_20260914.md` | GPU 重跑（R1FIX）結果與三個實測發現 |
| `CHBR-R1_GPU執行清單_20260914.md` | 該次重跑的前置作業清單 |

注意這些報告中的部分推論後來被本專案的實測修正，以本專案
`reports/工作報告_實驗重做完成_20260916.md` 的數字為準。例如原先認為
`num_ctx` 截斷是主要問題，實測顯示超過 2048 的僅 4 筆（0.04%），
真正的破口在 embedding 端的 `max_seq_length=256`（18.9%）。
